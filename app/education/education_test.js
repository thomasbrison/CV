'use strict';

describe('myCVApp.education module', function() {

  beforeEach(module('myCVApp.education'));

  describe('education controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var educationCtrl = $controller('EducationCtrl');
      expect(educationCtrl).toBeDefined();
    }));

  });
});
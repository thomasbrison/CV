'use strict';

describe('myCVApp.languages module', function() {

  beforeEach(module('myCVApp.languages'));

  describe('languages controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var languagesCtrl = $controller('LanguagesCtrl');
      expect(languagesCtrl).toBeDefined();
    }));

  });
});
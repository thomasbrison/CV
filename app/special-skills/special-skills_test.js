'use strict';

describe('myCVApp.specialSkills module', function() {

  beforeEach(module('myCVApp.specialSkills'));

  describe('special skills controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var specialSkillsCtrl = $controller('SpecialSkillsCtrl');
      expect(specialSkillsCtrl).toBeDefined();
    }));

  });
});
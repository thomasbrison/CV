'use strict';

describe('myCVApp.workExperience module', function() {

  beforeEach(module('myCVApp.workExperience'));

  describe('work experience controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var workExperienceCtrl = $controller('WorkExperienceCtrl');
      expect(workExperienceCtrl).toBeDefined();
    }));

  });
});